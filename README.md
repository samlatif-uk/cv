# CV — samlatif.uk

A minimal personal CV / portfolio website for Sam Latif.

Contents
- `index.html` — the site homepage (static HTML).

Usage
- To view locally, open `index.html` in your browser or run a simple HTTP server:

	Python 3:

	```bash
	python3 -m http.server
	```

Deploy
- This repository can be published via GitHub Pages — push to `main` and enable Pages in the repo settings.
- Live site: https://samlatif.uk (deployed)

Contact
- Email: hello@samlatif.uk

If you'd like additional sections (work history, projects, downloadable PDF, or notes for SEO), tell me what to include and I'll update this README and the site.
