export interface TechRow {
  cat: string;
  items: string;
  yrs: string;
}

export interface Skill {
  n: string;
  c: string;
}

export interface Job {
  co: string;
  date: string;
  title: string;
  desc: string;
  bullets: string[];
  stack: string[];
}
